const animals = [{ emoji: "🐶" }, { emoji: "🐱" }];
const newAnimals = animals.map((value) => value);

console.log(animals); // [{ emoji: "🐶" }, { emoji: "🐱" }]
console.log(newAnimals); // [{ emoji: "🐶" }, { emoji: "🐱" }]
